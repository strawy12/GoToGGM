using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class wae : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("¿¿æ÷");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
